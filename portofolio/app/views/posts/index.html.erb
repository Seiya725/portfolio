<h2>
  <%= link_to("Make a post", "/posts/new", class: "header-menu") %>
  Posts
</h2>
<ul>
  <% @posts.each do |post|%>
  <li> <%= link_to("#{post.title}", "posts/#{post.id}") %> </li>
  <% end %>
</ul>
